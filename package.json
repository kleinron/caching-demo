{
  "name": "cache-talk",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "npm run build && npm start",
    "start:origin": "npm run build && node dist/originServer.js",
    "start:proxy": "npm run build && node dist/proxyServer.js",
    "start:proxy1": "npm run build && node dist/proxyServer.js --port=4000",
    "start:proxy2": "npm run build && node dist/proxyServer.js --port=4001",
    "start:proxy3": "npm run build && node dist/proxyServer.js --port=4002",
    "start:router": "npm run build && node dist/routingProxy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.6.5",
    "express": "^4.18.2",
    "mime-types": "^2.1.35",
    "murmurhash3js-revisited": "^3.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/mime-types": "^2.1.4",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2"
  }
}
